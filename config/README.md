# Local Fiji Configuration

Copy `local_fiji_config.example.ps1` to `local_fiji_config.ps1` and set the local Fiji executable path. The local file is ignored because it contains machine-specific paths.

The example uses the checked-in deterministic dummy dataset. Repository-relative paths are supported for project files.
